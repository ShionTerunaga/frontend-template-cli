pre-commit:
    parallel: true
    commands:
        lint:
            run: pnpm lint:check
        format:
            run: pnpm format:check

commit-msg:
    commands:
        commitlint:
            run: pnpm exec commitlint --edit {1}

post-merge:
    commands:
        install:
            run: pnpm install
